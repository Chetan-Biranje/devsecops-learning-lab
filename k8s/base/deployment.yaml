apiVersion: apps/v1
kind: Deployment
metadata:
  name: devsecops-app
  namespace: devsecops
  labels:
    app: devsecops-app
    version: "2.0"
    managed-by: kustomize
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devsecops-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: devsecops-app
      annotations:
        # Force pod restart on config change
        checksum/config: "placeholder"
    spec:
      # ── Security Context (Pod level) ─────────────────
      securityContext:
        runAsNonRoot: true
        runAsUser:    1000
        runAsGroup:   1000
        fsGroup:      2000
        seccompProfile:
          type: RuntimeDefault

      automountServiceAccountToken: false

      # ── Anti-affinity: spread across nodes ───────────
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values: [devsecops-app]
                topologyKey: kubernetes.io/hostname

      containers:
        - name: app
          image: ghcr.io/chetan-biranje/devsecops-lab-app:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
              name: http
              protocol: TCP

          # ── Security Context (Container level) ─────────
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem:   true
            runAsNonRoot:             true
            runAsUser:                1000
            capabilities:
              drop: [ALL]

          # ── Resources ───────────────────────────────────
          resources:
            requests:
              cpu:    "100m"
              memory: "128Mi"
            limits:
              cpu:    "500m"
              memory: "512Mi"

          # ── Environment ─────────────────────────────────
          env:
            - name: FLASK_DEBUG
              value: "false"
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: secret-key

          # ── Health probes ────────────────────────────────
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds:       20
            failureThreshold:    3

          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds:       10
            failureThreshold:    3

          # ── Ephemeral storage ────────────────────────────
          volumeMounts:
            - name: tmp
              mountPath: /tmp

      volumes:
        - name: tmp
          emptyDir: {}

      # ── Image pull secret (GHCR) ─────────────────────
      imagePullSecrets:
        - name: ghcr-credentials
